# shellcheck disable=all
#
# Local environment template for the CredoAI Claude Code plugin marketplace.
#
# Setup:
#   cp example.env .local.env   # mise auto-loads .env and .local.env (see mise.toml)
#
# Most plugin authoring requires no environment variables. Add personal/local
# overrides here if you need them.
